# REST-API-users-balance
REST API (POST, GET). Application for managing user balances: deposit funds, withdraw funds, transfer money between users, get the current balance. (Works in PHP 5.3, PHP 8.0)
